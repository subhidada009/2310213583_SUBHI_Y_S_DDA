Week7. Homework

#include <stdio.h>

int main(){
    int a[10]={1,2,3,4},n=4,i,pos=2;

    for(i=n;i>pos;i--)
        a[i]=a[i-1];
    a[pos]=99; n++;

    for(i=pos;i<n;i++)
        a[i]=a[i+1];
    n--;

    for(i=0;i<n;i++)
        printf("%d ",a[i]);

    return 0;
}
